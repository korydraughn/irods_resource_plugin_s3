name: build-and-test-plugin-with-irods-packages

on: [push, pull_request]

defaults:
  run:
    shell: bash

jobs:
  build_and_test_plugin_with_prerelease_packages_using_default_matrix:
    uses: korydraughn/irods_reusable_github_workflows/.github/workflows/build-and-test-plugin-with-irods-packages-matrix.yml@c339e53b5e04cbd40f95dcc6a39c63b719c9d06e
